package terminal

// EnvTerminalSpawned is the environment variable used to prevent recursive
// terminal auto-spawning cascades when bg3-guard relaunches itself.
const EnvTerminalSpawned = "BG3_GUARD_TERMINAL"
